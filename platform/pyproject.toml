[project]
name = "graph-explorer-platform"
version = "0.1"
description = "Graph explorer platform"
dependencies = [
    "graph-explorer-api==0.1",
]

[tool.setuptools]
provides = ["graph-explorer-platform"]